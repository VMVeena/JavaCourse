       **********>Exception Handling ***********>
1. In Java, exception handling is a mechanism that allows a program to detect and respond to runtime errors (called exceptions) in a controlled way, 
  so that the normal flow of execution can continue without abrupt termination.
2. Exception will be hanfled in catch block.
3. One class have single try and multiple catch block.
4. once exption will happen in perticular line the next line in the try block will not execute, only catch block will execute.
5. If exption will not handled then application will terminate abnormally.
6. Whenever any exption will happen first it JVM will get notify about it, then JVM will will serach in that method whether any where it is handled or not , if not then it will chechk where it is called. 
  If there also its not handled then it will call default exption handler that will terminate application abnormally and giver summary to user.
<<<<<<< HEAD
7. Wnenver we wanted to throw excption to where it is calling even though after its handling  then we use "throw" keyword inside catch block.
8. Once the throw line is executed next lines will not execute. Excption will come to where method is called and there alos we need to handle this excption.(throw keyword will act as return statement)
9. If we want to inform about caller of the method about excption then we will use "throws" keyword. "throws" will not handle exception, its simply inform about exption may happen.
10. We can use "finally" block if we want to run the perticular block iirespective of whether excption is happened or not, or it is handled or not.
11. finally block we can use with only try block ,with try cath finally also.
12. Most of cases like if we want to close DB connection, file or some connection to be close there we will use finally block.


=======
7. If we want to throw exption to the mehtod where it si called even thought after handling it in catch block then we use "throw" key word inside cath block.
8. If we wrote throw inside catch block then we need to handle it in method also where we call it, else it aplication will terminate abnormally.
9. Once thow is excute the next line will not excuted, the controll will come to where its called.throw will act as return statement.
10. If we want to execute some bolck of code irrespective of whether exption is happened or not , if happned also whether it is handled or not then we need to use "finally" block.
11. We can use finally block with only try block, with try catch block also.
>>>>>>> branch 'main' of git@github.com:VMVeena/JavaCourse.git
